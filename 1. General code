#To install miniforge3 (download the installer first)
bash Miniforge3-Linux-x86_64.sh

#miniforge location: /rds/general/user/xl1720/home/miniforge3

#activate conda then check if successfully installed
source /rds/general/user/xl1720/home/miniforge3/bin/activate
conda --version

#install fastqc and check if installed
conda install bioconda::fastqc
which fastqc

#activate conda upon login
module load miniforge/3
eval "$(/rds/general/user/xl1720/home/miniforge3/bin/conda shell.bash hook)"
conda activate base
#There should be a (base) before each line of command now. 

#install samtools, bedtools, deeptools (after activating conda)
conda create -n align_env -c bioconda -c conda-forge samtools bedtools deeptools 
#They were installed together under environment align_env
#to activate use conda activate align_env


#to check if something exists
which thing
thing --version
